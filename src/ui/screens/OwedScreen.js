import { View } from "react-native";
import { Text } from "react-native-paper";

export default function OwedScreen({ navigation }) {
    return (
        <View >
            <Text>Owed Screen</Text>
        </View>
    );
}